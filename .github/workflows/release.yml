name: Release Package & Verification

on:
  push:
    tags:
      - 'v*'

permissions:
  contents: write

jobs:
  release:
    name: Build, Verify & Publish Release
    runs-on: ubuntu-latest
    timeout-minutes: 30

    steps:
      - name: Checkout Code
        uses: actions/checkout@v4

      - name: Set up OpenJDK 21
        uses: actions/setup-java@v4
        with:
          java-version: '21'
          distribution: 'temurin'
          cache: 'maven'

      - name: Build & Package Distribution (Assembly)
        run: |
          mvn clean package -DskipTests

      - name: Run Master Capstone Smoke Test (Unpack & Verify)
        run: |
          chmod +x scripts/*.sh
          ./scripts/run-release-smoke-test.sh
          ./scripts/verify-master-checklist.sh

      - name: GitHub Release Publication
        uses: softprops/action-gh-release@v1
        with:
          files: |
            target/aegisdb-*-bin.tar.gz
            target/aegisdb-*-bin.zip
            target/aegisdb-*-bin.tar.gz.sha256
            target/aegisdb-*-bin.zip.sha256
            target/bom.json
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
